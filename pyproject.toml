[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sdplab"
dynamic = ["version"]
description = "Tools for constructing and solving regularized semidefinite programming problems."
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [
  { name = "Pavlo Pelikh" }
]
dependencies = [
    "numpy",
    "scipy",
    "cvxpy",
    "spacecore>=0.4.2,<0.5",
]

keywords = [
  "semidefinite-programming",
  "conic-optimization",
  "convex-optimization",
  "regularization",
  "quantum",
  "jax",
  "numpy",
]

classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Science/Research",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Scientific/Engineering :: Mathematics",
]

[project.optional-dependencies]
jax = [
    "spacecore[jax]",
    "jax>=0.9.1",
    "optax>=0.2",
]
test = [
    "pytest>=8.0",
]

[project.urls]
Homepage = "https://github.com/Pavlo3P/SDPLab"
Repository = "https://github.com/Pavlo3P/SDPLab"
Issues = "https://github.com/Pavlo3P/SDPLab/issues"
Changelog = "https://github.com/Pavlo3P/SDPLab/blob/master/CHANGELOG.md"

[tool.setuptools.dynamic]
version = { attr = "sdplab._version.__version__" }

[tool.setuptools.packages.find]
include = ["sdplab*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
